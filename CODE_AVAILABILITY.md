# Code Availability

The `scripts/` directory contains the project scripts used to construct
harmonized domains, descriptive Gaussian-mixture partitions, guardrail analyses,
manuscript tables, workbooks, and submission checks. Some scripts require
restricted participant-level source files that are not redistributed here.

The included aggregate outputs and supplementary workbooks are sufficient to
inspect and reproduce the public-facing tables and figures without restricted
participant-level data.
